# Changelog

本文件记录 `feature/windows-server-supervisor` 分支相对上游 PST 的增强内容。项目继续按照 Apache License 2.0 发布，并保留原作者归属。

## [v0.14.17] - 2026-08-04

### Fixed

- 修复世界设置解析器只支持括号格式的列表字段（`CrossplayPlatforms`、`DenyTechnologyList`）的问题。现在也接受逗号分隔的裸列表格式，不再产生 `expected parenthesized list` 解析警告。
- 修复世界设置页保存时报 `AllowConnectPlatform is deprecated or reserved` 的问题。弃用/保留字段（deprecated/reserved）现在在页面中只读、不可修改，也不会提交给后端校验。

## [v0.14.16] - 2026-08-04

### Fixed

- 修复前端错误提示把后端英文技术错误吞成泛化的"校验失败"的问题。现在会透传真实错误文本，方便定位世界设置校验（`POST /api/world-settings/validate`）等接口返回 400 的具体原因；并补充 `palserver process management is not configured` 的中文翻译。

## [v0.14.15] - 2026-08-04

### Added

- QQ 机器人新增配种农场详情查询：发送"配种农场"可查看每个农场的亲本、蛋糕数量和蛋（含可孵化标记）。
- QQ 机器人新增公会查询：发送"公会列表"可查看公会等级、成员和据点数量。
- QQ 机器人新增据点详情查询：发送"XX据点详情"可查看据点等级、坐标、公会、工作帕鲁数和饲料箱。

### Fixed

- 修复保存据点名称时对不在当前存档快照中的据点（例如 SavSync 公会数据）返回 HTTP 404 的问题；现在允许保存别名，并仅在别名表内做重名校验。

## [v0.14.14] - 2026-08-04

### Added

- QQ 机器人支持查询离线玩家：发送"谁没在线"或"离线玩家"，列出当前不在线玩家及其最后在线时间和累计在线时长。
- QQ 机器人据点帕鲁查询支持列出据点全部工作帕鲁（不只是异常帕鲁），异常帕鲁优先展示并标记倒地、生病、受伤、饥饿、SAN 过低、生命过低等状态。

## [v0.14.13] - 2026-08-04

### Fixed

- 修复无参数工具（服务器状态、在线玩家、据点列表等）的 JSON Schema 中 `required` 被序列化为 `null` 的问题。JSON Schema 要求 `required` 必须是数组，DeepSeek 因此返回 HTTP 400 `null is not of type "array"` 拒绝所有带工具调用的请求。

## [v0.14.12] - 2026-08-04

### Fixed

- 修复 DeepSeek V4 多轮工具调用必然返回 HTTP 400 的问题。V4 默认开启 thinking 模式，带工具调用的 assistant 消息必须把 `reasoning_content` 原样回传，否则 DeepSeek 拒绝请求；现在会正确解析并回传该字段。
- DeepSeek 请求失败时透传官方返回的具体错误信息，不再只显示 HTTP 状态码。

## [v0.14.11] - 2026-08-04

### Fixed

- QQ 机器人 AI 调用失败时不再一律回复"DeepSeek 未配置"。现在会返回真实原因（API Key 无效、超时、额度不足等）并在日志中记录，便于定位问题。

## [v0.14.10] - 2026-08-04

### Added

- QQ 机器人新增“捣蛋喵”回复风格，可选择克制、活泼或调皮，并可在严重故障时自动切换为严肃语气。
- 固定命令、主动通知和可选 DeepSeek 共用同一人设配置；角色文案不会改写数量、时间、状态、错误或确认码。
- 地图和公会详情支持管理员直接重命名、重置据点名称；未命名据点按公会内顺序显示为“据点 1、据点 2……”并继续复用持久化别名。

### Fixed

- 修复库存后端只按存档英文名称和技术 ID 搜索的问题；网页和 QQ 机器人现在都支持中文、英文及技术 ID 查询，并统一返回中文物品名称。
- DeepSeek 模型更新为唯一受支持的 `deepseek-v4-flash`，已有 `deepseek-chat` 和 `deepseek-reasoner` 配置会自动迁移。

## [v0.14.9] - 2026-08-04

### Fixed

- 修复关闭崩溃守护时，已经到期但尚未真正启动的自动重启任务仍可能拉起 PalServer 的并发问题。

## [v0.14.8] - 2026-08-04

### Added

- 新增管理员“QQ 机器人”页面和 NapCatQQ + OneBot 11 正向 WebSocket 客户端，仅允许连接同机回环地址。
- 新增服务器、玩家在线时长、库存、据点、工作帕鲁、配种提醒、备份和自动重启计划的本地 QQ 命令，不依赖 AI。
- 新增据点改名和 PalServer 启动、平滑重启、保持停服命令，按管理员 QQ、独立权限开关和六位一次性验证码进行二次确认。
- 新增可选 DeepSeek Tool Calling，只注册固定查询、据点改名和 PalServer 控制工具；调用失败自动回退基础命令。
- 新增玩家上线/下线转换记录、群消息去重、用户/群限流、断线指数重连和有界主动通知队列。

### Security

- OneBot Token 和 DeepSeek API Key 保存后不回显，空值保留、显式确认后才能清除；日志、审计、URL 和前端状态缓存均不记录密钥。
- OneBot Token 只发往本机 NapCat 鉴权头；DeepSeek 请求会移除 IP、Steam/User ID、本机路径、密码、JWT 和技术错误详情。
- QQ 机器人不具备 Windows/PST 关机、RCON、Shell、SteamCMD、世界设置、备份恢复删除、公会或白名单修改能力。

### Removed

- 暂时移除 Production Bridge、生产订单页面及全部订单 API。当前运行时适配器与部分 Palworld/UE4SS 组合不兼容，可能影响服务器稳定性和存档解析。
- Windows Release 不再携带或安装 `PSTProductionBridge`。地图、备份、进程守护、自动重启、配种提醒、RCON、公会、白名单和中文运营台不受影响。

## [v0.14.7] - 2026-07-29

### Fixed

- 修复 Production Bridge 的 `Info.json` 不符合 Palworld 1.0 官方 Mod Loader 规范，导致重启后未部署 Lua、始终收不到心跳的问题。
- 修复 Bridge 心跳目录依赖 UE4SS 当前工作目录的问题；PST 现在向受管 PalServer 注入由可执行文件路径推导的固定 IPC 目录。
- 修复 Bridge 健康检查失败后，平滑关服返回成功但进程没有实际退出时维护事务持续超时的问题。
- 修复 Bridge 一键安装忽略 `PalModSettings.ini` 中 `WorkshopRootDir`、错误写入默认 `Mods\Workshop` 的问题。
- 修复“重新检测”只刷新旧状态的问题；现在会重新读取 Mod 配置、安装文件和运行时心跳，并清除上一轮维护遗留错误。

### Changed

- Bridge 安装包升级到 `0.1.1`；等待心跳最长时间调整为 5 分钟，并按官方部署清单、UE4SS 运行脚本和日志给出分阶段故障提示。
- 危险操作改为分级确认：平滑重启和停服使用二次确认弹窗；服务器更新、设置备份恢复、世界设置应用和 Mod 维护继续要求输入确认词。
- 总览按服主日常操作重排，首屏集中提供保存、重启、停服、备份和游戏内广播。
- 服务器运维改为标签按需加载；生产订单拆分为 Bridge 状态、新建订单和订单记录；世界设置增加常用设置与固定操作栏。
- 清理重复安全声明和装饰性英文标题，技术说明收进“了解更多”；网络请求增加 8 秒超时、离线提示、重试和统一中文错误转换。

### Security

- `WorkshopRootDir` 只从 PalServer 本地配置读取，前端不能指定；安装目标仍被限制在当前 PalServer 的 `Mods` 目录内。

## [v0.14.3] - 2026-07-29

### Added

- 新增管理员“生产订单”页面，可按“据点 → 具体工作台 → 配方 → 数量”预览实时材料并创建生产任务。
- 新增自研 `PST Production Bridge` 服务端 Mod，通过 PalServer 运行时生产接口下单，不修改世界存档，普通玩家无需安装客户端 Mod。
- Windows Release 固定携带 `extras/PSTProductionBridge`，PST 可检测 Bridge、UE4SS、文件哈希、版本、心跳和兼容能力。
- 新增 Bridge 安装、修复、安全禁用、生产目录、材料预览、订单查询、创建和取消 API，全部要求管理员 JWT。
- 新增本机文件 IPC、随机 Bridge 密钥、订单持久化、幂等派发、进度恢复和不确定状态保护。
- Release 工作流新增 Bridge 清单与 SHA-256 校验，并将 Bridge 一并放入 Windows 压缩包。

### Changed

- supervisor 增加受互斥锁保护的停机维护事务，可严格执行“保存 → 备份 → 平滑关服 → 等待退出 → 安装 → 恢复原运行状态”。
- 手动备份与 Bridge 安装复用同一套备份记录逻辑，记录来源、大小、状态和错误。
- PalServer 官方 REST API 的成功访问日志默认不再刷屏，错误与异常访问仍然保留。

### Security

- 浏览器不能提交下载地址、压缩包、任意文件路径、命令或脚本；安装源与目标目录均由 PST 固定推导。
- PST 不安装、升级或覆盖 UE4SS；缺少依赖、外部进程、非 Windows 平台、文件被修改或版本不兼容时安全拒绝自动操作。
- 安装器拒绝路径穿越、符号链接、清单外文件和哈希异常；失败时恢复 Bridge 目录和 `PalModSettings.ini`。
- Bridge 密钥、完整 IPC 请求和本机敏感路径不会进入普通日志、审计或配置响应。

## [v0.14.2] - 2026-07-27

### Added

- 新增统一“服务器运维”页面，集中管理 PalServer 进程、自动重启、SteamCMD 更新、存档备份、RCON、PST 日志与操作审计。
- 自动重启增加标准五段 Cron 模式、实时校验和未来三次执行时间预览，同时保留每天、每隔数日、每周和每月选择框。
- 新增严格受限的 SteamCMD 更新流程，App ID 固定为 `2394010`，依次执行保存、备份、关服、更新和重新启动。
- 新增滚动文件日志、有界内存日志接口和 `pst.db` 操作审计记录，统一对密码、JWT、令牌与 Authorization 信息脱敏。
- 新增管理员玩家成长进度接口和页面；仅展示真实存档中可验证的等级、经验、图鉴、探索、首领、科技和配方字段，不支持的字段返回未知。
- 新增手动存档备份接口；服务器运行时严格按照“保存世界后再压缩”的顺序执行。

### Changed

- 桌面端与移动端统一为紧凑中文专服运营台，导航调整为总览、玩家、地图、帕鲁管理、据点、库存、配种农场、服务器运维和世界设置。
- 玩家页整合在线管理、成长进度、公会、白名单和地图；据点页继续提供自定义名称与工作帕鲁入口。
- 保留全部旧 URL 和既有核心 API 行为；地图、配种提醒、RCON、白名单、公会、自动备份和崩溃守护继续可用。
- 补齐页面内部滚动、移动端底部导航、加载与空状态、危险操作确认和重复提交保护。

### Security

- SteamCMD 路径必须指向普通文件 `steamcmd.exe`，更新接口不接受 CMD、PowerShell、Shell 或自定义命令参数。
- 日志与审计接口均要求管理员 JWT，不把 JWT 放入 URL，也不保存敏感请求体。
- 更新失败后进入明确的 `UpdateFailed` 状态并暂停守护，避免在文件可能不完整时无限启动。

## [v0.14.1] - 2026-07-22

### Added

- 恢复独立世界地图入口，并将地图容器、边框、移动端高度和运营台导航统一为当前界面风格。
- 玩家列表与详情新增本次持续在线时长和累计在线时长；统计保存在 `pst.db`，PST 重启后继续累加。

### Fixed

- 修复全局页面禁止根滚动的问题，桌面鼠标滚轮、可见滚动条、移动端触摸滚动、侧栏和导航抽屉均可正常使用。
- 修复地图容器缺少确定高度导致 Leaflet 地图不可见的问题。

## [v0.14.0] - 2026-07-22

### Added

- 新增据点管理页面，管理员可按稳定据点 ID 设置、修改、重置自定义名称，并清理旧世界的失效名称记录。
- 据点别名持久化到 `pst.db` 的独立数据桶；重新解析存档和重启 PST 后仍然保留，不会写回 Palworld 存档。
- 新增据点别名管理员 API，以及重复名称、Unicode 长度和控制字符校验。

### Changed

- 工作帕鲁、库存位置、配种农场、历史提醒和游戏内产蛋广播统一使用当前据点显示名称，同时保留原始存档名称。
- 工作帕鲁、库存、配种农场与世界设置整合为统一中文管理中心，并增加桌面侧栏、移动导航和存档新鲜度状态条。
- 默认界面固定为简体中文，隐藏语言切换入口，并修复帕鲁 ID 大小写兼容及物品名称中的日文残留。

## [v0.13.0-enhanced.1] - 2026-07-22

### Added

- Windows 本地 `PalServer.exe` 并发安全进程管理器与明确状态机。
- 保存世界、启动、平滑停服、平滑重启和崩溃自动重启 API。
- 五分钟窗口内连续失败五次的默认崩溃循环保护，可由管理员重新启用。
- PST 启动时检测外部 `PalServer.exe` / `PalServer-Win64-Shipping-Cmd.exe` 进程。
- `config.db` 中的 `server_process` 配置组和严格可执行文件/参数校验。
- 桌面端与移动端共用的服务器进程状态卡、操作按钮和确认弹窗。
- 可即时更新的自动平滑重启配置，复用保存、关服、进程退出确认和延迟启动状态机。
- 进程卡片显示计划时区、下次执行、最近执行和最近调度结果。
- 自动重启周期扩展为每天、每隔数天、每周或每月，并提供无需手写 Cron 的选择式配置界面。
- 原子存档快照、据点工作帕鲁与饲料箱 API，以及桌面/移动自适应页面。
- 管理员只读全服库存聚合、位置与容器 API，以及按需加载详情页面。
- 随 PST 发布的 Palworld 1.0 世界设置字段表和可保留未知字段的 INI 状态机解析器。
- 世界设置校验、差异预览、受限备份、完整恢复、审计和平滑重启事务。
- 新配置启动或 REST 健康检查失败时恢复旧 INI 与旧 PST 连接配置。
- fake launcher 驱动的 supervisor 状态机、并发、鉴权、配置与敏感字段测试。
- 严格基于 MapObject、WorkSaveData 和专属容器 UUID 的配种农场、父母、蛋糕与待拾取蛋快照解析。
- 管理员配种农场桌面/移动页面、能力降级提示、筛选排序、站内通知中心和用户主动授权的浏览器通知。
- `config.db` 配种提醒范围设置，以及 `pst.db` 中跨重启持久化的蛋集合基线、唯一去重事件和已读历史。
- 配种新蛋事件通过 Palworld 官方广播接口发送到游戏内，并保留原有站内和浏览器提醒；广播模板支持据点与蛋数量占位符。
- 农场蛋糕容器以 `breeding_farm_cake_box` 独立来源进入全服库存并避免重复统计。

### Changed

- 官方 REST API 客户端增加 `/v1/api/save` 封装，平滑流程复用现有认证和 HTTP 客户端。
- 配置查询不再向前端返回 RCON 和 REST API 密码；前端提交空密码时保留原值。
- 原生 Release 工作流在打包前运行完整 Go 测试。
- `sav_cli` 在保留原玩家、公会同步的同时上传规范化据点与库存快照。
- 增强分支项目名调整为 `palworld-server-tool-enhanced`，下载、版本检测和 Release 链接指向维护者仓库。
- 默认重启、停服和世界设置恢复广播改为中文。

### Security

- 所有新增进程控制路由均位于现有 JWT 管理员路由组。
- 不提供任意命令执行能力，只允许白名单 PalServer 文件名和安全参数数组。
- 世界设置密码只返回设置状态；差异、审计与日志均不保存密码值。
- 世界设置文件路径由 PalServer 工作目录推导，备份 ID 严格校验并拒绝目录穿越。
